Function getLastRowByColumn(wsSheet As String, colLetter As String) As Long

    Dim sheet As Worksheet
    Set sheet = Worksheets(wsSheet)
    getLastRowByColumn = sheet.Range(colLetter & Rows.Count).End(xlUp).Row

End Function
